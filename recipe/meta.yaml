{% set name = "great_tables" %}
{% set version = "0.3.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/great-tables-{{ version }}.tar.gz
  sha256: e31f6d04c14fcaea831c8d292f5170b2fde5ac77ebc3192bab6040f6718b5252

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python >=3.7
    - setuptools >=45
    - setuptools-scm >=6.2
    - pip
  run:
    - python >=3.7
    - commonmark >=0.9.1
    - htmltools >=0.4.1
    - importlib-metadata
    - typing-extensions >=3.10.0.0
    - packaging >=20.9
    - pandas >=1.4.2
    - numpy >=1.22.4
    - babel >=2.13.1
    - mizani >=0.9.3
    - webcolors

test:
  imports:
    - great_tables
  commands:
    - pip check
  requires:
    - pip

about:
  home: https://github.com/posit-dev/great-tables
  summary: Easily generate information-rich, publication-quality tables from Python.
  doc_url: https://posit-dev.github.io/great-tables/
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - timkpaine
    - thewchan
